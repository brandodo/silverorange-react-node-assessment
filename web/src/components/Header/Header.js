import { Typography } from '@mui/material';

const Header = () => {
  return (
    <Typography variant="h2" sx={{ p: '20px' }}>
      List of Github Repos
    </Typography>
  );
};

export default Header;
